<!-- GitHub Repository Details -->
Project Name: Dev-Stack

Description: This Project help to explore frontend, backend, database, and tooling options, compare them side by side, and put together the stack that fits our next project.

Technology used:
1. React.js
2. Tailwind CSS
3. TypeScript / JavaScript (ES6+)
4. React-Toastify (NPM Package)
5. JSON (for technology data)
6. Vite (build tool)

3 features of the project:
1. Clean & Modern UI
2. State management & Fast data rendering.
3. Interective conditional UI Functionality.

<!-- React Related Answer -->
1. JSX is allow to write like HTML code with JavaScript.
2. Props: We can pass data from parents component to child components. Props is Immuable.
State: State can change with user activity. it's Mutable.
3. UseState is a default component of React that we can use to change/update data. We have used it to Update the Your Stack / Sidebar section in our project.
4. useEffect hook allow to mannage side effect of component. We need it because we can display the fetching data as soon as it loads.
5. Every item in a .map() list need a unique key prop because it's help to organize data in React Vartual DOM.
6. Conditional Rendering is showing UI according to the condition. We have use it Your Stack / Sidebar section in our project.
7. Parent component send the Props Using a Callback Function.Then Child Component send the data by calling this Function.

